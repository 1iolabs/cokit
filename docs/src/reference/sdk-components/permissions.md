# Permissions
## Facts

- We got two types of permissions:
    - Core Permissions - in the data model (logic).
    - Guards - a check that decides if an transaction is allowed.
        - Not allowed to to change the state. Because this is non deterministic.

### Core permissions


### Guards
can be programmed like a core into wasm
